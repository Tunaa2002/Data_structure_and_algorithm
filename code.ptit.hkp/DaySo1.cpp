#include <bits/stdc++.h>
using namespace std;

void in(){
    for(int i=0;i<n;i++) 
}

void quaylui(int i){

}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> a(n), b(n);
        for (int i = 0; i < n; i++)
            cin >> a[i];
        quaylui(1);
    }
}